var scene=new THREE.Scene,renderer=new THREE.WebGLRenderer({alpha:!0,antialias:!0});renderer.shadowMap.enabled=!0;var light=new THREE.DirectionalLight(new THREE.Color("rgb(255,255,255)")),geometry=new THREE.SphereGeometry(5,30,60),matertial=new THREE.MeshPhongMaterial({color:new THREE.Color("rgb(0,0,212)")}),mesh=new THREE.Mesh(geometry,matertial);mesh.position.set(0,0,0);renderer.setSize(640,480);document.body.appendChild(renderer.domElement);
var camera=new THREE.PerspectiveCamera(100,640/480,1,1E4);camera.position.y=.02;camera.position.z=10;scene.add(light);scene.add(mesh);renderer.render(scene,camera);function animate(){mesh.rotation.z+=.05;mesh.rotation.x+=.01;mesh.rotation.y+=.02;requestAnimationFrame(animate);renderer.render(scene,camera)}animate();
